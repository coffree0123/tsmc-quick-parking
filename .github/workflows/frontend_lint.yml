name: ESLint

on:
  pull_request:
    branches:
      - main
    paths:
      - frontend/**

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
        with:
          run_install: |
            - cwd: frontend
      - name: Run ESLint
        working-directory: frontend
        run: pnpm lint
