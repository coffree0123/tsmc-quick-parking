[tool.poetry]
name = "backend"
version = "0.1.0"
description = ""
authors = ["coffree <02320013apis@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
pre-commit = "^3.5.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
